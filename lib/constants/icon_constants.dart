import 'package:flutter/material.dart' show Icon, Icons;
import 'package:three_planets/constants/app_color_constants.dart';

const kRocketIcon = Icon(Icons.rocket_launch, color: kWhite);

const kArrowBackIcon = Icon(Icons.arrow_back, color: kWhite);

const kEarthIcon = Icon(Icons.public, color: kMyrtleGreenWithOpacity, size: 20);

const kCircleIcon = Icon(Icons.circle, color: kMyrtleGreenWithOpacity, size: 20);

const kFitnessCenterIcon = Icon(Icons.fitness_center, color: kMyrtleGreenWithOpacity, size: 20);

const kArrowDownIcon = Icon(Icons.arrow_downward, color: kMyrtleGreenWithOpacity, size: 20);

const kAtmosphereCompositionIcon = Icon(Icons.cloud, color: kMyrtleGreenWithOpacity, size: 20);

const kMoonsIcon = Icon(Icons.brightness_2, color: kMyrtleGreenWithOpacity, size: 20);

const k404NotFoundIcon = Icon(Icons.sentiment_very_dissatisfied, color: kMyrtleGreenWithOpacity, size: 100);
